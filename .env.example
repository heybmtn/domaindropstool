# Used only by tooling outside the Worker runtime (e.g. CI deploys via wrangler).
# Worker runtime secrets belong in .dev.vars (local) or `wrangler secret put` (production).
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
